
# Sivel
+ speedtest
binary = /usr/local/bin/speedtest-cli
timeout = 300
forks = 1
step = 3600
offset = random
pings = 3
extraargs = --secure

++ speedtest-download
measurement = download

++ speedtest-upload
measurement = upload

# Ookla official
+ speedtestcli
binary = /usr/local/bin/speedtest
timeout = 300
forks = 1
step = 3600
offset = random
pings = 3
# extraargs = --accept-license

++ speedtestcli-download
measurement = download

++ speedtestcli-upload
measurement = upload
